# 从零到一：不会编程的人，怎么用 AI 打造出 research-skill

> 一个完全不懂代码的人，花了几天时间，在 Claude Code 的辅助下，从一句"我想 7 天学会 Vibe Coding"开始，做出了一个全自动网页知识采集工具。

---

## 第一幕 · 起点

**"我想 7 天学会用 Claude Code 写程序。"**

我说了这句话。Claude 帮我搜了十几篇文章，我一篇篇手动复制到 Obsidian，然后发现——光整理这些文章就要花几个小时。

我当时就想：能不能说一句话，AI 自动帮我搜、帮我存、帮我整理成学习计划？

这就是 research-skill 的起点。不是先有技术方案，是先有自己遇到的真实问题。

## 第二幕 · 原型

第一版很快拼了出来。思路很简单：

```
WebSearch 搜索 → AskUserQuestion 勾选 → 逐篇剪藏到 Obsidian → 出 INDEX.md 总览 → 出 STUDYPLAN.md 学习计划
```

技术实现也简单：HTTP 下载网页 → 正则删 HTML 标签 → 写进 Obsidian。能用就行。

几天之内，我用这个工具收集了 15 篇 Vibe Coding 文章，自动生成了 INDEX.md（分类总览）和 STUDYPLAN.md（7 天学习计划）。

看起来很美好。然后我把 GitHub 链接发给了几个技术大佬。

## 第三幕 · 第一次危机

第一次点开 INDEX 里的文章，我的感觉很直接：

**"全是噪声，没法看。"**

然后我打开一看——导航栏文字混在正文里，侧边栏的"热门推荐"夹在段落中间，页脚的版权声明和联系方式出现在文章末尾。更别提那些广告链接和订阅引导。

我当时不好意思给别人看 GitHub 项目。

这触发了我对 quality 的第一次认真思考。AI 生成的东西，能用和好用之间，差的往往不是代码逻辑，而是这些细节。

## 第四幕 · 大佬点拨

一个技术大佬给了我两条建议：

**第一，换提取引擎。** 我的CC现在用的 `requests.get()` + `BeautifulSoup.get_text()`（用正则把 HTML 标签暴力删掉），太原始了。应该用 Mozilla 的 Readability——就是 Firefox 阅读模式背后的引擎。它能自动识别网页里哪个区域是正文，哪个是垃圾。

**第二，精排规则要具体。** 不能只说"清理噪声"，要列举每一项：导航栏、侧边栏、评论区、相关推荐、分享按钮、弹窗提示、订阅引导、版权声明……每一项都不能漏。

我不是很懂这些技术概念，但道理我听明白了——第一关就没把好，后面再怎么洗都是脏的。

## 第五幕 · 重构

在 Claude Code 辅助下，花了一个晚上完成重构。核心就一件事：**把 fetch-page.js 的提取逻辑从正则暴力删标签，换成 Readability 智能抽正文**。

### 抓取引擎

```
旧方案：HTTP GET → 正则删标签 → 噪声正文混在一起
新方案：HTTP GET → JSDOM 解析 → Readability 提取正文 → turndown 转 Markdown
```

Readability 自动剔除导航、侧边栏、广告、评论、页脚，只保留正文区域。如果 Readability 提取失败，自动回退到 DOM 选择器（`article`、`.content`、`main` 等）。

turndown 负责把干净的 HTML 转成 Obsidian 可渲染的 Markdown，保留标题层级、列表、代码块、表格、加粗格式。

### 浏览器回退路径

有些网站（SPA、反爬站点）HTTP 请求拿不到内容，需要浏览器渲染。

旧方案用 `agent-browser`，在 Windows 上一直弹 CMD 黑框、启动超时。修了三个点：
- Chrome 启动加 `--headless=new`，不依赖图形窗口
- 换 `puppeteer-core` 直连 CDP 协议，去掉 agent-browser 中间层
- 加 `--no-sandbox` 兼容 Linux

修完后整个链路：

```
HTTP GET → Readability → 成功？→ 输出干净 Markdown
                ↓ 失败
         Chrome CDP 渲染页面 → Readability → 输出干净 Markdown
```

### 仓库架构

之前 fetch-page.js 在独立仓库 `fetch-webpage` 里，没 git 管理。重构时直接合并进 research-skill：

```
旧：research/ + fetch-webpage/（两个仓库，独立安装）
新：research/（一个仓库，npm install 自动装依赖）
```

新用户只需 `git clone` + `bash install.sh`，零手动配置。

## 第六幕 · 打磨

引擎修好后，剩下的就是把规则补全——让 Claude 在精排时有明确的 checklist。

### SKILL.md 精排规则

**绝对排除**（发现即删除）：
- 导航栏、侧边栏、面包屑导航
- 广告、推广链接、赞助内容
- 评论区、留言区、用户反馈
- 相关推荐、热门文章、猜你喜欢
- 分享按钮、社交平台链接
- 弹窗提示、订阅引导、注册邀请
- 页脚版权声明、网站备案号、联系方式

**完整保留**：
- 标题、作者、发布时间
- 段落结构和层级小标题
- 有序/无序列表、代码块、表格、加粗/斜体

### 选择阶段过滤

搜索结果的展示阶段就主动标注并建议排除：
- 图书简介、新书推荐（需购买/借阅，信息量为零）
- 付费课程介绍、培训广告
- 内容不足 500 字的占位文章

### INDEX.md 改进

- 每篇文章标题用 Obsidian `[[文件名]]` 双链语法，点标题直接跳转
- 目标采集 8-12 篇，不足时自动换关键词补充搜索

## 第七幕 · 成果

### 改进前后对比

| 维度 | 改前 | 改后 |
|------|------|------|
| 正文提取 | 正则暴力删标签 | Readability 智能识别正文 |
| 导航栏残留 | 大量混入 | 零残留 |
| 代码块 | 丢失格式 | 完整保留，围栏正确 |
| 表格 | 丢失 | 完整保留 |
| 列表 | 编号跳跃 | 连续完整 |
| 浏览器回退 | 弹 CMD 窗，启动超时 | 静默 headless，秒级响应 |
| 安装步骤 | clone 两个仓库 | clone 一个，自动装依赖 |
| 图书广告 | 会被收录 | 自动过滤 |

### 测试：隋末唐初江都风俗

用重构后的 skill 跑了一个历史小说资料采集任务。搜索 → 勾选 8 篇 → 采集 → 精排 → 输出 INDEX。

结果：4 篇高质量正文直接可读，导航零残留。另外 4 篇自动过滤（1 篇图书简介、1 篇字数不足、2 篇站点反爬降级到浏览器路径失败）。

### 技术栈

```
@mozilla/readability  — 正文提取（Firefox 阅读模式引擎）
turndown              — HTML → Markdown
jsdom                 — Node.js 模拟 DOM
puppeteer-core        — Chrome CDP 连接
Obsidian REST API     — 知识库写入
```

---

## 关键启示

1. **先有痛点，再找方案。** 不是先决定"我要做个网页采集工具"，而是"我手动整理文章好麻烦"。

2. **AI 辅助开发的瓶颈不在代码。** 本次重构中 90% 的改动是 Claude 完成的。真正的决策点在于：什么时候该忍受、什么时候该重构、哪些建议该采纳。

3. **大佬的一句建议能省你一个月。** "用 Readability" 这句话本身只有几个字，但如果没有它，我可能会继续在正则规则上缝缝补补。

4. **品质问题是藏不住的。** 第一次被人点开 INDEX 看到满屏噪声，比什么测试报告都有说服力。

5. **一个仓库，一个安装命令。** 降低新用户的心智负担，比多写一个功能更有价值。

---

*2026 年 5 月，用 Claude Code 建造。*

*GitHub: [xuehai3-cyber/research-skill](https://github.com/xuehai3-cyber/research-skill)*
